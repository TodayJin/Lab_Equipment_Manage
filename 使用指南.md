# LabManager 使用流程

> 给实验室成员看的操作指南，照着走就行。

---

## 🖥️ 给服务器管理员

1. 双击 `admin.bat` 打开管理面板
2. 面板自动检测环境（Python / venv / 依赖），有问题点「🔧 一键修复」
3. 环境就绪后，点「▶ 启动服务」
4. 点「开机自启」→「开启自启」设置开机自动运行

管理面板 = 环境检测 + 一键修复 + 服务启停 + 实时日志 + 开机自启，全在一个窗口里。

---

## 快速上手（3 步）

1. **打开系统** → 浏览器输入管理员给的地址（如 `http://192.168.110.73:5000`）
2. **注册账号** → 点「立即注册」
3. **登录** → 用刚注册的账号登录
3. **开始使用** → 按下面流程图操作

---

## 完整操作流程

```
打开系统
  │
  ▼
┌──────────┐
│  登录/注册  │
└──────────┘
  │
  ▼
  ┌─────────────────────┐
  │  我要做什么？         │
  └─────────────────────┘
  │                    │
  ▼                    ▼
入库/出库            查看/查询
  │                    │
  ▼                    └──→ 去「仪表盘」看概览
┌──────────────────┐       去「记录查询」筛选
│ 器材存在吗？       │
└──────────────────┘
  │            │
  ▼ YES        ▼ NO
直接入库/出库  ┌──────────────┐
              │ 分类存在吗？   │
              └──────────────┘
                │          │
                ▼ YES      ▼ NO
              跳过      去「分类管理」
                        新建分类
                │          │
                └────┬─────┘
                     ▼
              ┌──────────────┐
              │ 去「器材管理」 │
              │ → 「新增器材」 │
              │ 填写名称/型号  │
              │ /封装/分类等   │
              └──────────────┘
                     │
                     ▼
              去「入库」或「出库」
              选择器材 → 填数量 → 确认
```

---

## 各功能操作说明

### 🔐 注册 & 登录

1. 打开浏览器访问系统地址（向管理员要）
2. 首次使用点「立即注册」，填用户名和密码
3. 已有账号直接登录

---

### 🏷️ 分类管理（有需要才操作）

> **什么时候需要？** 你要新增一个器材，但它的分类（如"玻璃器皿"）还不存在。

1. 点击左侧菜单「分类管理」
2. 在左侧表单填分类名称（如"玻璃器皿"、"电子设备"、"化学品"）
3. 点击保存
4. 分类出现在右侧列表中

---

### 📦 器材管理（有需要才操作）

> **什么时候需要？** 要入库或出库一种新器材，但这种器材在系统里还没有。

1. 点击左侧「器材管理」
2. 点击右上角「新增器材」
3. 填写信息：
   - **器材名称**（必填）：如"烧杯"
   - **型号**（选填）：如"B-500"
   - **封装**（选填）：如"箱装"
   - **分类**（必选）：下拉选择一个分类
   - **当前库存**：首次新增一般填 0
   - **预警阈值**：库存低于这个数会警告
   - **单位**：个/箱/瓶/台 等
4. 点击保存

---

### 📥 入库

1. 点击左侧「入库」
2. 在下拉列表中选择器材（会显示当前库存量，方便确认）
3. 填写入库数量
4. 可选填写备注（如"2024年采购批次 #3"）
5. 点击「确认入库」
6. 提示入库成功，库存自动增加

---

### 📤 出库

1. 点击左侧「出库」
2. 在下拉列表中选择器材
3. 填写出库数量
4. 如果库存不足，**系统会拒绝操作**并提示当前库存
5. 点击「确认出库」
6. 提示出库成功，库存自动减少

---

### 🔍 记录查询

1. 点击左侧「记录查询」
2. 按需设置筛选条件（可以组合使用）：
   - **操作人**：查某人的所有操作
   - **类型**：只看入库 或 只看出库
   - **器材**：只看某个器材的记录
   - **分类**：只看某一类器材的记录
   - **开始日期 / 结束日期**：限定时间范围
3. 点击「查询」
4. 结果展示：时间、类型、器材、数量、库存变化、操作人、备注

---

### 📊 仪表盘

首页概览，一眼看到：
- 器材种类总数
- 库存总量
- 本月入库/出库数量
- 库存不足的器材（红色标记）
- 最近 10 条操作记录

---

## 常见场景示例

### 场景 A：新买了一批烧杯，要入库

```
1. 看看「器材管理」里有没有"烧杯"
   ├── 有 → 直接去「入库」，选"烧杯"，填数量，确认
   └── 没有 →
       2. 看看「分类管理」有没有合适分类
          ├── 有 → 跳过
          └── 没有 → 新建一个分类（如"玻璃器皿"）
       3. 去「器材管理」→「新增器材」→ 填烧杯信息
       4. 去「入库」→ 选"烧杯"→ 填数量 → 确认
```

### 场景 B：同事小张要领取 5 个烧杯

```
1. 去「出库」→ 选"烧杯"
2. 填数量 5
3. 系统自动检查库存是否够
   ├── 够 → 确认，库存 -5
   └── 不够 → 提示"库存不足，当前只有 X 个"
4. 备注可以写"小张领取，用于实验 A"
```

### 场景 C：想知道小张这个月领了多少东西

```
1. 去「记录查询」
2. 操作人选"小张"
3. 类型选"出库"
4. 开始日期填本月 1 号
5. 点击查询 → 小张所有出库记录一目了然
```

---

## 注意事项

- ⚠️ 出库数量不能超过当前库存，系统会自动拦截
- ⚠️ 已有出入库记录的器材不能删除（保证日志完整）
- ⚠️ 下方有器材的分类不能删除
- 💡 入库/出库后建议填写备注，方便日后追溯
- 💡 设置「预警阈值」可以让库存不足的器材在仪表盘自动提醒
