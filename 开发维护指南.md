# LabManager 开发维护指南

> 给后续接手修改这个项目的人（或 AI）看的。

---

## 核心原则

### 1. 数据库兼容性

**数据库结构和数据不能丢。** 每次修改遵守以下规则：

- `models.py` 中的表结构尽量不做破坏性改动（删列、改类型）
- 新增字段用 `default=` 给默认值，让旧数据自动兼容
- **数据库文件是 `instance/lab.db`**，替换 exe 时保留 `instance/` 目录不动即可
- 启动时 `db.create_all()` 只创建新表，不删已有数据

```
替换 exe 的正确方式：
  旧目录                         新目录
  ├── LabManager.exe  ←替换→    ├── LabManager-V3.2.2.exe（新）
  ├── instance/       ←保留→    ├── instance/           （不动！）
  │   └── lab.db
  ├── static/uploads/ ←保留→    ├── static/uploads/     （不动！）
  └── settings.json
```

### 2. 数据在本地，不在云端

**不依赖外网。** 页面加载的所有资源都要放在 `static/` 目录里：

| 资源 | 位置 |
|------|------|
| Bootstrap CSS | `static/css/bootstrap.min.css` |
| Bootstrap JS | `static/js/bootstrap.bundle.min.js` |
| Bootstrap Icons | `static/css/bootstrap-icons.css` + `static/fonts/` |
| Chart.js | `static/js/chart.umd.min.js` |

**禁止在模板里写 CDN 链接**（如 `cdn.jsdelivr.net`），否则断网时页面白屏。

---

## 项目结构

```
lab-inventory/
├── admin.bat              # 双击打开管理面板
├── run.py                 # 服务器启动入口
├── build.py               # PyInstaller 打包脚本
├── requirements.txt       # Python 依赖
│
├── src/                   # 后端 Python 源码
│   ├── app.py             # Flask 应用工厂，注册蓝图
│   ├── models.py          # 数据库模型（改表结构看这里）
│   ├── config.py          # 全局配置
│   ├── forms.py           # WTForms 表单定义
│   ├── auth.py            # 登录/注册
│   ├── dashboard.py       # 仪表盘
│   ├── equipment.py       # 器材管理
│   ├── categories.py      # 分类管理
│   ├── stock.py           # 入库/出库
│   ├── records.py         # 记录查询
│   ├── lab.py             # 签到/公告/值日/备份/统计API
│   ├── chat.py            # 群聊/文件传输/分片上传/P2P
│   ├── account.py         # 用户账户
│   ├── analytics.py       # 数据分析
│   ├── admin.py           # 管理面板（桌面程序）
│   ├── admin_web.py       # 网页端管理后台
│   ├── helpers.py         # 权限装饰器 / 操作日志
│   ├── settings.py        # 系统设置
│   ├── settings_routes.py # 设置路由
│   └── about.py           # 关于页面
│
├── templates/             # Jinja2 HTML 模板
│   ├── base.html          # 全局布局（侧边栏/顶栏）
│   ├── auth/              # 登录/注册/改密
│   ├── dashboard/         # 仪表盘
│   ├── equipment/         # 器材管理
│   ├── categories/        # 分类管理
│   ├── stock/             # 入库/出库
│   ├── records/           # 记录查询
│   ├── lab/               # 签到/公告/值日/备份/统计
│   ├── chat/              # 群聊 + 共享文件
│   ├── admin/             # 管理后台
│   ├── analytics/         # 数据分析
│   ├── account/           # 账户页面
│   ├── settings/          # 设置页面
│   └── about/             # 关于页面
│
├── static/                # 前端静态资源
│   ├── css/
│   │   ├── style.css      # 全局样式（CSS变量/动画/组件）
│   │   ├── bootstrap.min.css
│   │   ├── bootstrap-icons.css
│   │   └── dark.css       # 暗色模式
│   ├── js/
│   │   ├── app.js         # 全局脚本（Toast/续签/滚动/P2P轮询/未读消息轮询）
│   │   ├── searchable-select.js  # 可搜索下拉组件
│   │   ├── bootstrap.bundle.min.js
│   │   └── chart.umd.min.js
│   ├── fonts/             # 图标字体
│   └── uploads/           # 用户上传文件（头像/聊天/P2P/shared）
│
├── instance/              # 运行时数据（替换exe时保留！）
│   ├── lab.db             # SQLite 数据库
│   └── settings.json      # 系统设置
│
├── dist/                  # 打包输出
├── docs/                  # 设计文档
│
├── 说明文档.md
├── 架构文档.md
├── 使用指南.md
└── 开发维护指南.md
```

---

## 开发流程

### 标准流程

```
1. 提需求 → AI 分析代码 → 设计方案 → 确认
2. AI 修改源码（src/ templates/ static/）
3. 本地测试：venv\Scripts\python run.py
4. 验证通过 → 打包：venv\Scripts\python build.py
5. 将 dist/LabManager.exe 发给用户替换旧 exe
6. 用户保留 instance/ 和 uploads/ 目录不动
```

### 打包命令

```bash
venv\Scripts\python build.py
```

如果提示文件被占用，先关掉正在运行的 LabManager.exe 再打包。

---

## 前端开发注意事项

### 中文输入法兼容

**所有带实时搜索/输入监听的 input 框**，必须处理 `compositionstart` / `compositionend`：

```javascript
let composing = false;
input.addEventListener('compositionstart', () => { composing = true; });
input.addEventListener('compositionend', () => { composing = false; });
input.addEventListener('input', () => {
    if (!composing) doSearch();
});
```

**compositionend 之后不要立即提交表单**，等 input 事件自动触发即可，否则页面刷新导致光标消失。

Enter 键提交在拼音阶段也要阻止：

```javascript
input.addEventListener('keydown', (e) => {
    if (composing) return;
    if (e.key === 'Enter') submit();
});
```

### 数据传递方式

**优先在页面渲染时嵌入 JSON**，避免 AJAX 请求的 500/HTML 错误：

```html
<script>
const DATA = {{ data_dict | tojson }};
// 前端直接使用，不请求网络
</script>
```

### 表单提交后滚动位置

全局已做滚动恢复（`app.js`），POST 表单提交前记录 `scrollY`，页面加载后恢复。

### CSS 变量

定义在 `static/css/style.css` 的 `:root` 里，改主题色直接改变量。

---

## 后端开发注意事项

### 权限控制

```python
from src.helpers import admin_required

@route('/admin/something')
@login_required
@admin_required
def admin_only():
    ...
```

**所有删除操作必须加 `@admin_required`**（器材、分类、记录、公告、用户）。

### 操作日志

```python
from src.helpers import log_operation
log_operation('action_name', 'target_type', target_id, target_name, detail_dict)
```

### 数据库变更

1. 在 `models.py` 添加模型/列
2. 给旧数据兼容的默认值
3. `db.create_all()` 只创建新表，不修改已有表（SQLite 限制）
4. 破坏性变更需在 `app.py` 的 `create_app()` 写迁移逻辑

---

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 替换exe后数据丢了 | instance/ 没保留 | 保留 instance/ 目录 |
| 页面加载很久 | 引用了 CDN 资源 | 下载到 static/ 本地引用 |
| 中文输入搜一下就打断 | 没处理 composition 事件 | 见上方「中文输入法兼容」 |
| 打包报 PermissionError | exe 正在运行 | 关掉 LabManager.exe 再打包 |
| 大文件 P2P 传不过去 | 单次 POST 太大 | 分片上传 (10MB/片) |
| AJAX 返回 HTML 错误 | 服务端 500 异常 | 加 try-except 返回 JSON，或内嵌数据 |
| 通知权限不弹 | 局域网 IP 不支持 | Chrome 要求 localhost 或 HTTPS |

### V3.1~ 新增

| 功能 | 位置 |
|------|------|
| 搜索下拉组件 | `static/js/searchable-select.js` + `src/stock.py` API |
| 续签检测 (全局) | `static/js/app.js` (LabRenew) |
| 签到页倒计时 | `templates/lab/checkin.html` |
| 签到统计 (内嵌数据) | `templates/lab/checkin_stats.html` + `src/lab.py` |
| 分片上传 | `templates/chat/files.html` + `src/chat.py` (`/chunk-upload`) |
| P2P 全局轮询 | `static/js/app.js` (`/chat/p2p/pending`) |
| Toast 通知 | `static/js/app.js` (LabToast) |
| 蜂鸣声提醒 | `static/js/app.js` (playBeep, Web Audio API) |

### V3.2 新增

| 功能 | 位置 |
|------|------|
| 未读消息红点 | `src/chat.py` (`/unread` API) + `src/models.py` (`last_read_chat_id`) + `static/js/app.js` (全局轮询) + `templates/base.html` (侧边栏徽标) |
