# LabManager — 实验室器材管理系统

## 功能介绍

- 👥 **用户系统**：注册账号、登录/登出
- 📦 **器材管理**：名称、型号、封装、分类、库存数量、预警阈值
- 🏷️ **分类管理**：按类型组织器材（玻璃器皿、电子设备、化学品等）
- 📥 **入库**：填写数量、封装、型号等信息，库存自动增加，生成操作日志
- 📤 **出库**：校验库存后扣减，生成操作日志
- 🔍 **高级筛选**：按操作人、出/入库类型、器材、分类、时间范围自由组合查询
- ⚠️ **库存预警**：库存低于阈值时高亮提醒
- 📊 **仪表盘**：库存概览、预警列表、最近操作记录
- 💬 **群聊 & 文件**：实验室群聊（未读红点提醒）、共享文件、P2P 大文件传输
- 📋 **签到系统**：签到/签退、续签确认、自动签退、在线时长统计
- ⏰ **值日排班**：每周排班管理
- 📢 **公告板**：实验室公告发布

## 技术栈

- Python 3 + Flask
- SQLite 数据库
- Bootstrap 5 界面
- 无需额外数据库安装，开箱即用

## 快速开始

### 方式一：exe 直接运行（推荐，无需安装任何东西）

把 `dist/LabManager.exe` 发给别人，双击即用。**不需要装 Python，不需要装任何依赖。**

### 方式二：源码运行

**双击 `admin.bat`**，管理面板会自动检测环境：

- 缺少虚拟环境 → 点「一键修复」
- 缺少依赖包 → 点「一键修复」
- 环境就绪 → 点「▶ 启动服务」

一切都在面板里完成，不需要命令行。

> 想自己打包 exe：`venv\Scripts\python build.py`，输出到 `dist/LabManager.exe`

## 文件说明

```
lab-inventory/
├── admin.bat              # 双击打开管理面板（一切入口）
├── run.py                 # 服务器启动入口（面板内部调用）
├── requirements.txt       # Python 依赖
│
├── src/                   # 源代码 (15个模块)
│   ├── admin.py           # 管理面板程序
│   ├── app.py             # Flask 应用工厂
│   ├── config.py          # 配置
│   ├── models.py          # 数据模型 (13张表)
│   ├── forms.py           # 表单定义
│   ├── helpers.py         # 权限装饰器/操作日志
│   ├── settings.py        # 系统设置读写
│   ├── auth.py            # 登录/注册
│   ├── dashboard.py       # 仪表盘
│   ├── equipment.py       # 器材管理
│   ├── categories.py      # 分类管理
│   ├── stock.py           # 入库/出库
│   ├── records.py         # 记录查询
│   ├── lab.py             # 签到/公告/值日/备份
│   ├── chat.py            # 群聊/文件/P2P/未读
│   ├── account.py         # 账户管理
│   ├── analytics.py       # 数据分析
│   ├── admin_web.py       # 网页管理后台
│   ├── settings_routes.py # 设置路由
│   └── about.py           # 关于页面
│
├── templates/             # Jinja2 HTML 模板
├── static/                # CSS/JS
├── instance/              # SQLite 数据库 (自动生成)
│
├── 架构文档.md            # 项目架构
├── 使用指南.md            # 用户操作流程
└── 说明文档.md            # 本文件
```
