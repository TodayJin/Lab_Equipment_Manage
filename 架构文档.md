# LabManager V3.1 项目架构文档

> 实验室器材管理系统 — Flask + SQLite + Bootstrap 5

## 目录总览

```
lab-inventory/
├── admin.bat              # 管理面板入口 (双击)
├── run.py                 # 服务器启动入口
├── build.py               # PyInstaller 打包脚本
├── requirements.txt       # Python 依赖清单
├── .gitignore
├── 说明文档.md            # 部署说明
├── 架构文档.md            # 本文件
├── 使用指南.md            # 用户操作指南
├── 开发维护指南.md         # 开发参考
│
├── src/                   # 后端 Python 源码
│   ├── admin.py           # 管理面板程序 (启停/日志/自启)
│   ├── app.py             # Flask 应用工厂，注册 13 个 Blueprint
│   ├── config.py          # 全局配置 (密钥/DB/上传限制)
│   ├── models.py          # 数据库模型 (13 张表)
│   ├── forms.py           # WTForms 表单定义
│   ├── helpers.py         # 权限装饰器 / 操作日志工具
│   ├── settings.py        # 系统设置存储
│   ├── auth.py            # 认证蓝图 (登录/注册/改密)
│   ├── dashboard.py       # 仪表盘蓝图
│   ├── equipment.py       # 器材管理蓝图 (CRUD+搜索+级联删除)
│   ├── categories.py      # 分类管理蓝图
│   ├── stock.py           # 入库/出库蓝图 + 器材搜索API
│   ├── records.py         # 记录查询蓝图 + 管理员删除
│   ├── lab.py             # 签到/公告/值日/备份/统计API
│   ├── chat.py            # 群聊/文件传输/分片上传/P2P
│   ├── account.py         # 用户账户管理
│   ├── analytics.py       # 数据分析
│   ├── admin_web.py       # 网页端管理后台
│   ├── settings_routes.py # 设置路由
│   └── about.py           # 关于页面
│
├── templates/             # Jinja2 HTML 模板
│   ├── base.html          # 全局布局 (侧边栏+顶栏+脚本)
│   ├── auth/              # 登录/注册/修改密码
│   ├── dashboard/         # 仪表盘
│   ├── equipment/         # 器材管理 (列表+表单)
│   ├── categories/        # 分类管理
│   ├── stock/             # 入库/出库 (可搜索下拉)
│   ├── records/           # 记录查询 (管理员删除)
│   ├── lab/               # 签到/公告/值日/备份/统计
│   ├── chat/              # 群聊 + 共享文件
│   ├── admin/             # 管理后台 (用户/日志)
│   ├── analytics/         # 数据分析
│   ├── account/           # 账户设置
│   ├── settings/          # 个性化设置
│   └── about/             # 关于 (v3.1)
│
├── static/                # 前端静态资源 (全部本地化)
│   ├── css/
│   │   ├── style.css      # 全局样式 (CSS变量/动画/组件)
│   │   ├── bootstrap.min.css
│   │   ├── bootstrap-icons.css
│   │   └── dark.css
│   ├── js/
│   │   ├── app.js         # 全局脚本 (Toast/续签/滚动/P2P轮询)
│   │   ├── searchable-select.js  # 可搜索下拉组件
│   │   ├── bootstrap.bundle.min.js
│   │   └── chart.umd.min.js
│   ├── fonts/             # 图标字体
│   └── uploads/           # 用户上传文件
│       ├── p2p/           # P2P 点对点文件 (自动清理)
│       ├── shared/        # 固定共享文件
│       ├── chat/          # 群聊图片/文件
│       └── tmp/           # 分片上传临时文件
│
├── instance/              # 运行时数据 (替换exe时保留)
│   ├── lab.db             # SQLite 数据库
│   └── settings.json
│
├── dist/                  # 打包输出
│   └── LabManager-V3.1.exe
│
└── docs/                  # 设计文档
    └── superpowers/specs/
```

---

## 数据库模型

### User — 用户表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | 自增主键 |
| username | String(80) | 用户名 (唯一) |
| password_hash | String(200) | Werkzeug 密码哈希 |
| role | String(20) | admin / member |
| avatar | String(200) | 头像文件名 |
| created_at | DateTime | 注册时间 |

### UserSettings — 用户设置
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| user_id | FK→User | 用户 |
| dark_mode | Boolean | 深色模式 |
| items_per_page | Integer | 每页条数 |

### Category — 分类表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| name | String(80) | 分类名 (唯一) |
| description | Text | 描述 |

### Equipment — 器材表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| name | String(200) | 器材名称 |
| model | String(200) | 型号 |
| packaging | String(200) | 封装 |
| category_id | FK→Category | 所属分类 |
| stock_quantity | Integer | 当前库存 (仅通过入库/出库修改) |
| alert_threshold | Integer | 预警阈值 |
| unit | String(50) | 单位 |
| remark | Text | 备注 |
| updated_at | DateTime | 更新时间 |

### StockRecord — 出入库记录
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| equipment_id | FK→Equipment | 器材 |
| user_id | FK→User | 操作人 |
| type | String(10) | in / out |
| quantity | Integer | 数量 |
| before_stock | Integer | 操作前库存 |
| after_stock | Integer | 操作后库存 |
| remark | Text | 备注 |
| undone | Boolean | 是否已撤销 |

### OperationLog — 操作日志
记录所有管理操作，用于追溯。

### Notice — 公告
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| title | String(200) | 标题 |
| content | Text | 内容 |
| is_pinned | Boolean | 是否置顶 |
| user_id | FK→User | 发布者 |

### DutyDay — 值日排班
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| day_of_week | Integer | 0=周一..6=周日 |
| user_ids | String(500) | 逗号分隔的用户ID |
| remark | String(200) | 备注 |

### Attendance — 每日签到
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| user_id | FK→User | 成员 |
| date | Date | 日期 |

### AttendanceLog — 签到/签退事件
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer PK | |
| attendance_id | FK→Attendance | 签到记录 |
| sign_in_time | DateTime | 签到时间 |
| sign_out_time | DateTime | 签退时间 (NULL=在线中) |

### ChatMessage — 群聊消息
### SharedFile — 共享文件
### P2PTransfer — 点对点传输

---

## Blueprint 路由

### auth — 认证 (`/`)
| 路由 | 说明 |
|------|------|
| `/login` | 登录 |
| `/register` | 注册 |
| `/logout` | 登出 |
| `/change-password` | 修改密码 |

### dashboard — 仪表盘 (`/`)
| 路由 | 说明 |
|------|------|
| `/` | 仪表盘：公告栏+值日+统计卡片+预警+最近操作 |

### equipment — 器材管理 (`/equipment`)
| 路由 | 说明 |
|------|------|
| `/` | 列表 (实时搜索+分类过滤+排序+分页) |
| `/new` | 新增 (库存固定0，通过入库增加) |
| `/<id>/edit` | 编辑 (不可改库存) |
| `/<id>/delete` | 删除 (**管理员**，级联删除关联记录) |

### categories — 分类管理 (`/categories`)
| 路由 | 说明 |
|------|------|
| `/` | 列表+新增 |
| `/<id>/edit` | 编辑 |
| `/<id>/delete` | 删除 (**管理员**) |

### stock — 库存操作 (`/stock`)
| 路由 | 说明 |
|------|------|
| `/in` | 入库 |
| `/out` | 出库 |
| `/api/equipment/list` | JSON: 器材列表 (供搜索下拉组件) |
| `/undo/<id>` | 撤销操作 (5分钟内) |

### records — 记录查询 (`/records`)
| 路由 | 说明 |
|------|------|
| `/` | 6维筛选+分页 |
| `/<id>/delete` | 删除单条记录 (**管理员**，回退库存) |
| `/export` | 导出CSV |

### lab — 实验室管理 (`/lab`)
| 路由 | 说明 |
|------|------|
| `/notices` | 公告列表 (**发布/删除需管理员**) |
| `/duty` | 值日排班 (成员只读，管理员编辑) |
| `/checkin` | 签到页面 |
| `/checkin/signin` | 签到 |
| `/checkin/<id>/signout` | 签退 |
| `/checkin/auto-signout` | 自动签退 API |
| `/checkin/status` | 签到状态 API |
| `/checkin/stats` | 统计页面 |
| `/checkin/user/<id>/detail` | 成员详情 API |
| `/backup` | 数据备份 (**管理员**) |

### chat — 群聊 & 文件 (`/chat`)
| 路由 | 说明 |
|------|------|
| `/` | 群聊 |
| `/messages` | 新消息轮询 API |
| `/send` | 发送消息 (AJAX) |
| `/files` | 共享文件页面 |
| `/files/upload` | 上传共享文件 |
| `/chunk-upload` | **分片上传** (支持大文件) |
| `/cancel-upload` | 取消上传 |
| `/p2p/send` | P2P 发送 (小文件) |
| `/p2p/pending` | P2P 轮询 API (全局通知) |
| `/p2p/<id>/download` | 下载 P2P 文件 |
| `/heartbeat` | 在线心跳 |

---

## 关键特性

### 续签机制 (V3.1)
- 签到后 localStorage 存时间戳，全标签页共享
- 满 3/6/9 小时弹续签确认窗 (页面内+蜂鸣声)
- 5分钟倒计时不响应自动签退
- 12小时硬性自动签退
- 签到页实时显示「本次在线」和「续签倒计时」

### 分片上传 (V3.1)
- 文件切成 10MB 片逐片上传，支持 5GB
- 可取消传输
- P2P 记录最多 5 条/人，1 天自动清理

### 权限控制
- 管理员专属：器材删除、分类删除、记录删除、公告管理、备份、用户管理
- 成员：只能查看值日排班，不可编辑

### 数据兼容
- 替换 exe 保留 `instance/` 和 `uploads/` 即可
